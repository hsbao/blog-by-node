<%- include('header') %>
	<% if (result.length) { %>
		<% result.forEach(function (item) { %>
		  	<%- include('commo/post-content', { post: item }) %>
		<% }) %>
	<% } else { %>
		<div style="width: 50%;margin: 40px auto;text-align: center; font-size: 16px;">暂无用户发布文章..</div>
	<% } %>
<%- include('footer') %>
